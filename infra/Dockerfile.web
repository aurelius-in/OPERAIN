FROM node:20-alpine

WORKDIR /app/web

COPY web/package.json web/package-lock.json* ./
RUN npm install || (echo "No web package.json yet; using dev volume" && exit 0)

COPY web ./

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]

